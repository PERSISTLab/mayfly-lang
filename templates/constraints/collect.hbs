			{{#isCollect}}
			// Handled by the task itself and edge data available status byte
			// Collect: is their enough data ({{collectsize}} data)
			//if(__{{source}}_output.num < {{collectsize}}) {
			//	constraints_satisfied = 0;
			//}{{/isCollect}}
